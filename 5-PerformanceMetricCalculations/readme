The following code is run for all the difference feature sets. In addition to printing the Metrics, this also creates the ML models which are further used for testing.

sudo python3 label_preprocess_create_model.py
