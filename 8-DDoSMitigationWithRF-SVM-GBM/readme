#Models from 5-PerformanceMetricCalculations
#See subfolders
#Threshold 0.8 meaning a probability above 80% would block packets. This could be tweaked.

#The following code is ran 6 times, twice for each model with and without mitigation, specified in each sub and subsubfolder.
#CLI1> ryu-manager custom_ryu_controller.py --config-file ryu-config.conf
#CLI2> sudo python3 custom_topology.py --remote-controller
#mininet> h13 hping3 -2 -V -d 120 -w 64 --rand-source --flood h15
