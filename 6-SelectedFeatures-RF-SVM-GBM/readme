#ML Models produced in 5-PerformanceMetricsCalculations/

#Following code in each subfolder
#CLI1> ryu-manager custom_ryu_controller.py --config-file ryu-config.conf
#CLI2> sudo python3 traffic_gen_norm_ddos.py --remote-controller
#CLI>sudo python3 graph.py
#Test with RF also captures CPU, see specified code in subfolder.
